# Same EmailJS config as .env.production — Next.js only loads
# .env.production during `next build` (production mode), not `next dev`,
# so local dev needs its own copy of these to exercise the contact form.
# See .env.production for why these are safe to commit.

NEXT_PUBLIC_EMAILJS_SERVICE_ID=service_51oea0i
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=template_xngldvi
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=ga2ddD00dIVW8M-4P
